version: '3'

services:
  postgres:
    image: postgres:16
    environment:
      - 'POSTGRES_DB=etutor_hierarchical_clustering'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=etutor_hierarchical_clustering'
    ports:
      - '5434:5432'
